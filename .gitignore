# macOS Finder droppings
.DS_Store

# Python
__pycache__/
*.pyc

# Scan output — regenerate it, never commit someone's findings
scan-*.json
Security_Report_*.html
Security_Report_*.xlsx

# ตัวก่อนแก้ เก็บไว้เทียบในเครื่อง ไม่ต้องขึ้น repo
dist/
